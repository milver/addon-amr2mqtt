---
name: Stale

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  workflows:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: Alvarofg/addon-workflows/.github/workflows/stale.yaml@main
