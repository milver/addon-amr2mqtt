---
name: Sync labels

permissions:
  contents: read

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "34 5 * * *"
  workflow_dispatch:

jobs:
  workflows:
    uses: Alvarofg/addon-workflows/.github/workflows/labels.yaml@main
