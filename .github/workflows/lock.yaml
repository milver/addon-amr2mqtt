---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

jobs:
  workflows:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: Alvarofg/addon-workflows/.github/workflows/lock.yaml@main
